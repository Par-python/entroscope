## Summary

What does this PR do, and why?

Closes #

## Changes

-

## Checklist

- [ ] Tests added or updated (and `pytest` passes locally)
- [ ] `ruff check` and `ruff format --check` are clean
- [ ] New public functions follow the existing `compute` / `rolling` / `delta` /
      `plot` API where applicable
- [ ] Docstrings / README / CHANGELOG updated if behavior or the public API changed
- [ ] For a new measure: correctness is checked against a known analytic value,
      not just "looks reasonable"

## Notes for reviewers

Anything worth flagging: tradeoffs, open questions, areas you want eyes on.
